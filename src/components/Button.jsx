import React from "react";
import './ButtonStyle.css';

const Button = () => {
    return(
        <div className="btns">
            <button id="contact">Contact</button>
        </div>
    )
}

export default Button;